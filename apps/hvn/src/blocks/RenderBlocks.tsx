import { RenderContainerBlocks } from './common/RenderContainerBlock'
import { ContainerBlockProps } from './common/type'

export const RenderBlocks = (props: ContainerBlockProps) => {
  return <RenderContainerBlocks {...props} />
}
